# --- 비전 & 멀티모달 ---
opencv-python>=4.8.0
mediapipe>=0.10.14
pillow>=10.0.0

# --- 음성 인식 (STT) & 오디오 입력 ---
faster-whisper>=1.0.0
sounddevice>=0.4.6
scipy>=1.11.0
numpy>=1.24.0

# --- AI 두뇌 엔진 & 스키마 검증 ---
google-genai>=0.1.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# --- 데이터베이스 ---
psycopg2-binary>=2.9.0
pgvector>=0.2.5

# --- RAG 장기 기억 (로컬 경량 임베딩) ---
fastembed>=0.3.0

# --- 화자 식별 (Speaker Verification, 경량 CPU 추론) ---
resemblyzer>=0.1.4

# --- 음성 합성 (TTS) & 오디오 출력 ---
edge-tts>=6.1.0
pygame>=2.5.0

# --- 테스트 및 유틸리티 ---
pytest>=7.4.0